#include "main.h"
#include <stdio.h>

/**
 * main- prints argc
 * @argc: count
 * @argv: array of arguments
 * Description: prints number of arguments
 * Return: integer
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	argv = argv;
	return (0);
}

