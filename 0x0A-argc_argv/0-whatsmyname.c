#include "main.h"
#include <stdio.h>

/**
 * main - prints name of file
 * @argc: number of arguments
 * @argv: arguments entered
 * Return: integer
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	argc = argc;

	return (0);
}

