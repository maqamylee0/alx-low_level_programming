#include "main.h"

int _isdigit(int c)
{
        return (0);
}
int _strlen(char *s)
{
        return (1);
}
void _puts(char *s)
{
        write(1, &s, 1);
}
char *_strcpy(char *dest, char *src)
{
        return (dest);
}
int _atoi(char *s)
{
        int i = 0;
        return (i);
}
